async function test() { const loginRes = await fetch('http://localhost:8081/api/v1/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'superadmin@example.com', password: 'admin123' }) }); const loginData = await loginRes.json(); console.log('Login:', loginData.token ? 'Success' : loginData); if (!loginData.token) return; const res = await fetch('http://localhost:8081/api/v1/clientes', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + loginData.token, 'x-tenant-id': loginData.user.tenantId || 'demo-tenant' }, body: JSON.stringify({ name: 'Test Client ' + Date.now(), email: 'client_' + Date.now() + '@example.com', createUser: true }) }); const data = await res.json(); console.log('Cliente created:', data._generatedPassword); } test().catch(console.error);